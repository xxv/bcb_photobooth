object returned via https://www.google.com/calendar/feeds/lnkgrvbbsgteqoie6kbtok8fro%40group.calendar.google.com/public/basic?alt=json
!!! (Use: <script src="https://www.google.com/calendar/feeds/lnkgrvbbsgteqoie6kbtok8fro%40group.calendar.google.com/public/basic?alt=json-in-script&callback=loadTalks"></script>)
contains an array of event objects

this array is at object.feed.entry
    each array object has a number of properties,
    object.content.$t is the easiest to use,

    content.split("<br />"), yeilds an array of:

    [When, Where, Status(not needed)]

    object.title.$t Gives us the name,
    title.split(":")[1] strips off the room name / location
    (alternatively, we can grab the location here instead of from content)

    Org these into a collection of objects or an array,
    grab local time to calculate what to initailly show and add a datepicker  

    Trace map into svg
